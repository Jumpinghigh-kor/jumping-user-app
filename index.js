/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';

// app.json 파일을 사용하지 않고 직접 앱 이름 지정
const appName = 'jumpingUserApp';

AppRegistry.registerComponent(appName, () => App);
